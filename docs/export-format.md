# Exportformat

Das ZIP enthält ein versionsgebundenes Manifest, je Seite HTML, Markdown, Originalquelle und JSON-Metadaten sowie Original-Attachments. Reports werden als HTML und JSON erzeugt. Alle Eintragsnamen werden bereinigt und relativ erzeugt; absolute Quellpfade dienen ausschließlich zum kontrollierten Lesen.
